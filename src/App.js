import "./App.css";

function App() {
  // const [user] = useAuthState(auth);

  return (
    <div className="App">
      <header className="App-header">
        {/* <section>{user ? <ChatBox /> : "sign ins"}</section> */}
        <SignIn />
        {/* Sally */}
      </header>
    </div>
  );
}

export default App;
